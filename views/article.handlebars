<div class="row"> 
 {{!--  {{#each curntArt}} --}}
  <h3 class="card">{{this.title}}</h3>
  <h5 class="card-panel">{{this.link}}</h5>
  {{!-- {{/each}} --}}
</div>

<div class="row">
   <form action="" method="POST" class="col s12 card-panel">
      <div class="row">
        <div class="input-field">
          <i class="material-icons prefix">perm_identity</i>
          <input id="icon_prefix" type="text" value="" class="validate">
          <label for="icon_prefix">Name</label>
        </div>
        <div class="input-field">
          <i class="material-icons prefix">comment</i>
          <input id="icon_telephone" type="text" value="" class="validate">
          <label for="icon_telephone">Comment</label>
        </div>
      </div>
    </form>
</div

{{!-- comments go here --}}
